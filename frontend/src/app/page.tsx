// ══════════════════════════════════════════════════════════════
// فایل page.tsx — صفحه ریشه (Root Page)
//
// وقتی کاربر آدرس "/" رو باز می‌کنه (یعنی فقط localhost:3000)
// این صفحه اجرا میشه.
//
// کار این صفحه خیلی سادهست:
// فوری بیمار رو به صفحه لاگین هدایت می‌کنه.
// redirect یه تابع Next.js هست که کاربر رو به آدرس دیگه‌ای
// می‌فرسته — بدون اینکه صفحه‌ای نشون داده بشه.
// ══════════════════════════════════════════════════════════════

// redirect = تابعی که کاربر رو فوری به صفحه دیگه‌ای میفرسته
import { redirect } from "next/navigation";

// ──────────────────────────────────────────────────────────────
// RootPage: هر بار که "/" بازه میشه، فوری به "/login" میره
// هیچ چیزی رندر نمیشه — صفحه خالیه
// ──────────────────────────────────────────────────────────────
export default function RootPage() {
  redirect("/login");
}
